# BLACKHEAD Android workspace

The Android application is a local control surface for `blackheadd`, not a second source of runtime truth. Kotlin communicates through generated BHCP types and authenticated IPC. The Gradle baseline and package namespace are added in the Android foundation milestone.
