name: Release
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Release publishing is intentionally disabled."
          echo "Signing, SBOM, provenance, compatibility, and rollback gates are required."
          exit 1
