name: CIAOS QEMU
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  scaffold:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo "CIAOS QEMU validation remains inactive until the adapter is executable."
