# Magisk release payload

Release automation places the signed `blackhead-fallback.apk`, `payload-manifest.json`, and `payload-manifest.sig` here while assembling the module ZIP. No fake APK or detached signature is committed.
