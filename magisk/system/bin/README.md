# Runtime binaries

Verified release builds place `blackheadd`, `bhctl`, and `bh-probe` here. Text placeholders are prohibited because the installer must never mistake a stub for an executable runtime.
